# Rasheh—fixed Vercel build
Delete all old repository contents, upload this folder's contents, and do not use vercel.json.
Vercel environment variables: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, ADMIN_EMAIL.
Test: /api/health must return ok:true before testing registration.
